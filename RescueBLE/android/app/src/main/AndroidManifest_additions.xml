<!-- Agregar al AndroidManifest.xml dentro de <manifest> -->
<!-- Archivo: android/app/src/main/AndroidManifest.xml -->

<!--
  PERMISOS BLUETOOTH
  Android 12+ (API 31+): BLUETOOTH_SCAN, BLUETOOTH_CONNECT, BLUETOOTH_ADVERTISE
  Android < 12: ACCESS_FINE_LOCATION (BLE scanning lo requiere)
-->

<!-- Bluetooth clásico (compatibilidad) -->
<uses-permission android:name="android.permission.BLUETOOTH"
    android:maxSdkVersion="30" />
<uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
    android:maxSdkVersion="30" />

<!-- Bluetooth moderno (Android 12+) -->
<uses-permission android:name="android.permission.BLUETOOTH_SCAN"
    android:usesPermissionFlags="neverForLocation" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
<uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

<!-- Ubicación (requerida para BLE scan en Android < 12) -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

<!-- Para mantener BLE activo en background -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

<!-- Wake lock para mantener CPU activa durante escaneo en background -->
<uses-permission android:name="android.permission.WAKE_LOCK" />

<!-- Feature BLE -->
<uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

<!--
  FOREGROUND SERVICE para escaneo/advertising en background (Android 8+)
  Agregar dentro de <application>:

  <service
      android:name=".BLEForegroundService"
      android:enabled="true"
      android:exported="false"
      android:foregroundServiceType="connectedDevice" />
-->
